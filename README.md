# 🚀 Cpp_Lessons: C++ 

Bu depo, C++ (C Plus Plus) öğrenim sürecimde aldığım notları, ders örneklerini ve internetten "bu neymiş?" diyip kurcaladığım projeleri barındırır.

## 📂 İçerik ve Konular

Bu depoda C++'ın derinliklerine daldığım şu konuları bulabilirsiniz:

* **Temel C++:** `cin`, `cout`, `namespace` kavramları
* **Nesne Yönelimli Programlama (OOP):**
    * **Sınıflar ve Nesneler (Classes & Objects):** Kendi tiplerimizi oluşturma sanatı.
    * **Kapsülleme (Encapsulation):** `private`, `public`, `protected` (Kimse değişkenlerime dokunamaz!)
    * **Miras Alma (Inheritance):** "Baba malı tez tükenir" ama kodda miras candır.
    * **Çok Biçimlilik (Polymorphism):** Bir fonksiyon, bin marifet.
* **Bellek Yönetimi:** `new` ve `delete` operatörleri (Manuel vites kullanmayı seviyoruz.)
* **STL (Standard Template Library):**
    * `vector` (Dizilerin modern hali), `list`, `map`
    * Algoritmalar (`sort`, `find` vs.)
* **Dosya İşlemleri:** `fstream` ile dosya okuma/yazma.
* **Exception Handling:** Hata yakalama (`try`, `catch`, `throw`) - Program patlamasın diye.

## 🛠️ Kurulum ve Çalıştırma

Bu C++ kodlarını çalıştırmak için makinenizde `g++` derleyicisi kurulu olmalı.

1.  **Depoyu klonlayın:**
    ```bash
    git clone [https://github.com/arslan468/Cpp_Lessons.git]
    cd Cpp_Lessons
    ```

2.  **Bir dosyayı derleyin (Örnek):**
    ```bash
    g++ ornek_dosya.cpp -o ornek_uygulama
    ```

3.  **Çalıştırın:**
    * Windows: `ornek_uygulama.exe`
    * Linux/Mac: `./ornek_uygulama`


## 📝 Lisans

Bu proje tamamen eğitim ve "kendini geliştirme" amaçlıdır.